version: 2.1

jobs:
  run-build:
    steps:
      - run:
        name: Deploy Over SSH
        command: |
          ssh $SSH_USER@$SSH_HOST "cd /var/www/production && git checkout ./ && git pull && npm run build"

workflows:
  build-workflow:
    jobs:
      - run-build:
        branches:
          only: main